<% content_for :title, t("participate.survey.already_answered") %>

<style>
  body { background: linear-gradient(135deg, #f8faff 0%, #f0f4ff 50%, #faf5ff 100%); min-height: 100vh; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }
  .fade-in { animation: fadeIn 0.4s ease forwards; }
</style>

<div class="min-h-screen flex flex-col">

  <div class="flex-1 flex items-center justify-center px-4 pb-10">
    <div class="w-full max-w-[480px] fade-in text-center">
      <div class="rounded-2xl py-12 px-8" style="background:rgba(255,255,255,0.85); border:1px solid rgba(99,102,241,0.15); box-shadow:0 8px 32px rgba(99,102,241,0.08); backdrop-filter:blur(12px);">
        <div class="w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-5"
             style="background:linear-gradient(135deg,#6366f1,#a855f7);">
          <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
          </svg>
        </div>
        <h1 class="text-[22px] font-extrabold text-slate-800 mb-2"><%= t("participate.survey.already_answered") %></h1>
        <p class="text-[13px] text-slate-500 leading-relaxed">
          <%= t("participate.survey.already_answered_desc") %>
        </p>
        <% if @survey&.title.present? %>
          <p class="text-[12px] text-slate-400 mt-3 font-medium"><%= @survey.title %></p>
        <% end %>
      </div>
    </div>
  </div>

  <div class="text-center pb-6">
    <span class="inline-flex items-center gap-1.5"><img src="/vox_logo.svg" alt="VOX" style="height:18px;width:auto;opacity:0.65;"><span style="font-size:11px;color:#94a3b8;">· Survey &amp; Voting Platform</span></span>
  </div>
</div>
